apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: three-tier
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 80         # service port
      targetPort: 80 # container port
      nodePort: 30080  # optional, agar specify karna ho
