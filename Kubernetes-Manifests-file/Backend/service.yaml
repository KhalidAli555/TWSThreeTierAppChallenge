apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: three-tier
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500   # choose a port in 30000-32767 range
